import { DrinkCategoryNamePipe } from './drink-category-name.pipe';

describe('DrinkCategoryNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DrinkCategoryNamePipe();
    expect(pipe).toBeTruthy();
  });
});
